name: betafpv-ultrathink
channels:
    - conda-forge
    - defaults
dependencies:
    - python=3.11
    - pip
    - pip:
          # Existing dependencies (updated to latest stable versions)
          - pyserial
          - pymavlink
          - opencv-python
          - pillow
          - numpy
          - tk

          # RL and Training dependencies
          - gymnasium
          - pufferlib
          - torch
          - pybullet
          - tensorboard
          - pyyaml
          - wandb

          # Utilities
          - tqdm
          - matplotlib
